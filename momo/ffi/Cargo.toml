[package]
name = "momo-ffi"
version = "0.3.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
momo = { path = "..", default-features = false, features = ["local-embeddings"] }
serde_json = "1"
